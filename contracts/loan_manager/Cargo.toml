[package]
name = "loan_manager"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
remittance_nft = { path = "../remittance_nft" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
